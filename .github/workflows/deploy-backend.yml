name: Deploy Backend Only

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to deploy (e.g., v1.0.0)'
        required: true
        type: string

jobs:
  build-and-push-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Prepare lowercase owner
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ env.OWNER_LC }}
          password: ${{ secrets.REGISTRY_PAT }}

      - name: Derive SHORT_TAG (drop leading v only)
        run: |
          TAG="${{ inputs.tag }}"
          if [[ "$TAG" == v* ]]; then
            echo "SHORT_TAG=${TAG#v}" >> $GITHUB_ENV
          else
            echo "SHORT_TAG=$TAG" >> $GITHUB_ENV
          fi

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ env.OWNER_LC }}/pere-sapin-backend:${{ inputs.tag }}
            ghcr.io/${{ env.OWNER_LC }}/pere-sapin-backend:latest
            ghcr.io/${{ env.OWNER_LC }}/pere-sapin-backend:${{ env.SHORT_TAG }}
          build-args: |
            NODE_ENV=production

  deploy-backend:
    needs: build-and-push-backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: "latest"

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: "latest"

      - name: Set up Kubeconfig
        run: |
          echo "$KUBECONFIG_DATA" > kubeconfig
        env:
          KUBECONFIG_DATA: ${{ secrets.KUBECONFIG_DATA }}

      - name: Prepare lowercase owner
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - name: Deploy backend with Helm
        run: |
          helm upgrade --install backend ./helm-charts/backend \
            --set image.repository=ghcr.io/${{ env.OWNER_LC }}/pere-sapin-backend \
            --set image.tag=${{ inputs.tag }} \
            --set secrets.jwtSecret="${{ secrets.JWT_SECRET }}" \
            --kubeconfig kubeconfig